# flagpole-io-api

API that facilitates interaction with the system data store.

## Getting Started

1. Install the project dependencies

    ```shell
    npm install
    ```
2. Run the project

    ```shell
    npm run dev
    ```